# Проверка диска каждые 5 минут
*/5 * * * * /opt/ops/disk_watch.sh

# Резервная копия конфигурации каждый день в 02:00
0 2 * * * /opt/ops/backup-config.sh

# Архивация старых логов каждое воскресенье в 03:00
0 3 * * 0 /opt/ops/archive-logs.sh

# Отчёт для передачи смены каждый день в 08:00
0 8 * * * /opt/ops/generate-handover.sh
